// import types from './types'

// const INITIALAMOUNT = { amount: 0};

// function amount(state = INITIALAMOUNT, action) {
//     switch(action.type) {
//         case types.ADD:
//             return {...state, amount: state.amount + action.value}
//         case types.TAKE:
//             return {...state, amount: state.amount - action.value}
//         case types.RESET:
//             return {...state, amount: 0}
//         default:
//             return state;
//     }
// };

// export default { amount }